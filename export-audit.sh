#!/bin/bash

# Create output file
OUTPUT_FILE="full-code-audit.txt"

# Start with header
{
    echo "════════════════════════════════════════════════════════════════════"
    echo "CRYPTOBRAINNEWS - COMPLETE CODE EXPORT FOR CLAUDE OPUS AUDIT"
    echo "════════════════════════════════════════════════════════════════════"
    echo "Export Date: $(date)"
    echo "Tech Stack: Next.js 16, React 19, TypeScript, Tailwind, Supabase"
    echo "════════════════════════════════════════════════════════════════════"
    echo ""
    
    # 1. Package.json (verify versions)
    echo "═══════════════════════════════════════════════════════════════════"
    echo "📦 FILE: package.json"
    echo "═══════════════════════════════════════════════════════════════════"
    cat package.json 2>/dev/null || echo "❌ package.json not found"
    echo ""
    echo ""
    
    # 2. Folder Structure
    echo "═══════════════════════════════════════════════════════════════════"
    echo "📁 PROJECT FOLDER STRUCTURE"
    echo "═══════════════════════════════════════════════════════════════════"
    if command -v tree &> /dev/null; then
        tree -I 'node_modules|.next|dist|.git' --dirsfirst
    else
        echo "Tree command not found. Structure not generated."
    fi
    echo ""
    echo ""
    
    # 3. DUNE_QUERIES.md
    echo "═══════════════════════════════════════════════════════════════════"
    echo "📋 FILE: DUNE_QUERIES.md"
    echo "═══════════════════════════════════════════════════════════════════"
    if [ -f "DUNE_QUERIES.md" ]; then
        cat DUNE_QUERIES.md
    else
        echo "❌ DUNE_QUERIES.md not found"
    fi
    echo ""
    echo ""
    
    # 4. supabase_schema.sql
    echo "═══════════════════════════════════════════════════════════════════"
    echo "🗄️  FILE: supabase_schema.sql"
    echo "═══════════════════════════════════════════════════════════════════"
    if [ -f "supabase_schema.sql" ]; then
        cat supabase_schema.sql
    else
        echo "❌ supabase_schema.sql not found"
    fi
    echo ""
    echo ""
    
    # 5. .env.example (check env vars)
    echo "═══════════════════════════════════════════════════════════════════"
    echo "🔐 FILE: .env.example"
    echo "═══════════════════════════════════════════════════════════════════"
    if [ -f ".env.example" ]; then
        cat .env.example
    else
        echo "❌ .env.example not found"
    fi
    echo ""
    echo ""
    
    # 6. All TypeScript/TSX/CSS files from src
    echo "═══════════════════════════════════════════════════════════════════"
    echo "🔧 SOURCE CODE FILES (src/)"
    echo "═══════════════════════════════════════════════════════════════════"
    echo ""
    
    if [ -d "src" ]; then
        find src -type f \( -name "*.tsx" -o -name "*.ts" -o -name "*.css" \) | sort | while read file; do
            echo "───────────────────────────────────────────────────────────────────"
            echo "FILE: $file"
            echo "───────────────────────────────────────────────────────────────────"
            cat "$file"
            echo ""
            echo ""
        done
    else
        echo "❌ src directory not found"
    fi
    
    # 7. Config files from root
    echo "═══════════════════════════════════════════════════════════════════"
    echo "⚙️  CONFIG FILES"
    echo "═══════════════════════════════════════════════════════════════════"
    
    for config in next.config.ts tsconfig.json tailwind.config.ts postcss.config.mjs eslint.config.mjs; do
        if [ -f "$config" ]; then
            echo ""
            echo "───────────────────────────────────────────────────────────────────"
            echo "FILE: $config"
            echo "───────────────────────────────────────────────────────────────────"
            cat "$config"
        fi
    done
    
    # 8. Sentry configs
    echo ""
    echo "═══════════════════════════════════════════════════════════════════"
    echo "📊 SENTRY CONFIGURATION"
    echo "═══════════════════════════════════════════════════════════════════"
    for sentry in sentry.client.config.ts sentry.server.config.ts sentry.edge.config.ts; do
        if [ -f "$sentry" ]; then
            echo ""
            echo "───────────────────────────────────────────────────────────────────"
            echo "FILE: $sentry"
            echo "───────────────────────────────────────────────────────────────────"
            cat "$sentry"
        fi
    done
    
    echo ""
    echo "════════════════════════════════════════════════════════════════════"
    echo "✅ EXPORT COMPLETE"
    echo "📊 Total files included: $(grep -c "^FILE:" $OUTPUT_FILE 2>/dev/null || echo "N/A")"
    echo "════════════════════════════════════════════════════════════════════"
    
} > "$OUTPUT_FILE"

echo "✅ Code exported to $OUTPUT_FILE"
echo "📊 File size: $(du -h $OUTPUT_FILE | cut -f1)"
echo "📝 Lines: $(wc -l < $OUTPUT_FILE)"
