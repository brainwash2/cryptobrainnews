import React from 'react';

export default function AuthCallbackPage() {
  return (
    <div className="min-h-screen bg-[#0a0a0a] flex items-center justify-center">
      <div className="text-[#a3a3a3] font-mono text-xs uppercase tracking-[0.3em]">
        Authentication callback not available — Supabase removed.
      </div>
    </div>
  );
}